<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="76dp"
    android:layout_height="76dp"
    tools:layout_editor_absoluteX="99dp"
    tools:layout_editor_absoluteY="43dp">

    <ImageButton
        android:id="@+id/noteImageButton"
        android:layout_width="49dp"
        android:layout_height="47dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="14dp"
        android:layout_marginTop="6dp"
        android:layout_marginEnd="13dp"
        android:layout_marginBottom="23dp"
        android:background="@drawable/note"
        android:clickable="false" />

    <TextView
        android:id="@+id/noteTitleTextView"
        android:layout_width="49dp"
        android:layout_height="16dp"
        android:layout_alignStart="@+id/noteImageButton"
        android:layout_alignTop="@+id/noteImageButton"
        android:layout_alignEnd="@+id/noteImageButton"
        android:layout_alignBottom="@+id/noteImageButton"
        android:layout_marginStart="1dp"
        android:layout_marginTop="49dp"
        android:layout_marginEnd="-1dp"
        android:layout_marginBottom="-18dp"
        android:gravity="center"
        android:text="ЗАМЕТКА"
        android:textColor="#000000"
        android:textSize="16sp" />
</RelativeLayout>